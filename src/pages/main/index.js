import React, { Component, Fragment } from 'react';
import Map from '../map';

class Main extends Component {
  render() {
    return (
      <Fragment>
        <Map />
      </Fragment>
    );
  }
}

export default Main;
